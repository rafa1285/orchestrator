# flows/

This folder contains n8n workflow definitions (exported as JSON).

Each file represents one automation flow that can be imported directly into an n8n instance.
Flows coordinate agents, external services, and MCP tools to automate tasks end-to-end.
